# API
# Private: used server-side and in next.config.ts rewrites (never exposed to the browser)
API_URL=http://localhost:5173
# Public: used only for direct browser navigations (e.g. OAuth redirects)
NEXT_PUBLIC_API_URL=http://localhost:5173

# Feature Flags
NEXT_PUBLIC_AUTH_ENABLED=true
NEXT_PUBLIC_BETA_MODE=false
